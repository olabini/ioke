
IKIL(
  ; Should create a native Rescue class in ioke.lang or Ioke.Lang
  ; Should add an init method that sets the name
  ; Should set the mimic to be Origin
  ; Should set the object name on Ground correctly

  Handler = IokeObject(Origin)
  Rescue  = IokeObject(Origin)
)
